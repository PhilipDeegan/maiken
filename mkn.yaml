

name: mkn
scm: https://github.com/dekken/maiken

parent: static
mode: none

profile:
  - name: static
    inc: ./inc
    src: ./src
    dep: 
      - name: mkn.kul
        version: master
      - name: parse.yaml
        version: stable
    main: cpp.cpp
    mode: static

  - name: shared
    parent: static
    mode: shared
    arg: -DYAML_CPP_DLL
