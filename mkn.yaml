

name: mkn

parent: static
mode: none

profile:
  - name: static
    inc: ./inc
    src: ./src
    dep: 
      - name: mkn.kul
        version: master
      - name: parse.yaml
        version: 0.5.5
    main: dbg.cpp
    mode: static

  - name: shared
    parent: static
    mode: shared
    arg: -DYAML_CPP_DLL
